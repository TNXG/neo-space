{
  "name": "@neo-space/backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "cargo watch --no-process-group --ignore cache --ignore target --ignore '**/*.md' --ignore '**/*.sh' -x run",
    "build": "cargo build --release",
    "build:debug": "cargo build",
    "start": "cargo run --release",
    "lint": "cargo clippy --all-targets --all-features -- -D warnings",
    "lint:fix": "cargo clippy --all-targets --all-features --fix --allow-dirty --allow-staged",
    "typecheck": "cargo check --all-targets --all-features",
    "clean": "cargo clean"
  },
  "dependencies": {
    "@neo-space/admin": "workspace:*"
  }
}
